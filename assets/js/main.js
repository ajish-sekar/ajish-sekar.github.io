$(document).ready(function(){
    // intitialize side nave
    $('.sidenav').sidenav();
    // open side nav in mobile
    $('.sidenav').sidenav('open');
    
    // initialize tooltip
    $('.tooltipped').tooltip();
    
    // intitialize scrollspy
    $('.scrollspy').scrollSpy();
  });
